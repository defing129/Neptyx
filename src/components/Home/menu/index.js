import React from "react";
import styled from "styled-components/native";


export default function MenuHeader({color, width, height, border, background, text, icon, borderColor,navigation}) {

    return (
        <MainBottom>

        </MainBottom>
    )
}
const MainBottom  = styled.View `
`
